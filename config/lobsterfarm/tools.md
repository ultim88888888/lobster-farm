# TOOLS.md — Machine Infrastructure

_Environment-specific configuration. Generated by `lobsterfarm init`, updated as infrastructure evolves._

---

## Machine

- **Name:** {{MACHINE_NAME}}
- **Hardware:** {{MACHINE_HARDWARE}}
- **Sudo:** {{SUDO_STATUS}}
- **Permissions:** {{PERMISSIONS_STATUS}}

> Sudo and permissions are configured during `lobsterfarm init`. See setup docs for manual configuration.

## Secrets (1Password)

- **CLI:** `op`
- **Service account token:** stored in `~/.zshrc` as `OP_SERVICE_ACCOUNT_TOKEN`
- **App usage:** `op run --env-file .env.op -- <command>`
- **Script usage:** `op read "op://vault/item/field"`

### Vault Structure
- **Master vault** (`lobsterfarm`): Shared service credentials (Vercel, Sentry, domain registrar, etc.) — services that manage multiple entities under one account.
- **Per-entity vaults** (`entity-{id}`): Entity-specific secrets (API keys, database credentials, third-party integrations unique to that entity).

## Shared Services

_Services that operate at the account level, managing multiple entities._

{{#SHARED_SERVICES}}
{{/SHARED_SERVICES}}

> Example entries (added during setup or as services are connected):
> - **Vercel:** account `my-org` — deploys all entity frontends
> - **Sentry:** org `my-org` — error tracking across all entities
> - **Domain registrar:** Cloudflare account — DNS for all entities

---

_This file grows as we add services. Keep it factual — what exists, where it lives, how to access it._
